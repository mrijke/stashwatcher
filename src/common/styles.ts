import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    backgroundColor: "#fff",
  },
  loading: {
    flex: 1,
    alignItems: "center",
  },
  cameraPreview: {
    flex: 1,
    justifyContent: "flex-end",
    alignItems: "center",
  },
});
